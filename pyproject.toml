[project]
name = "claudometer"
version = "0.1.0"
description = "Lightweight local tracker for Claude AI usage"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.58.0",
    "python-dotenv>=1.2.2",
    "requests>=2.32.5",
]
